---
description: 批量制作素材时必须一次只做一个，禁止并行/批量提交提示词
alwaysApply: true
---

# 批量内容制作：一次一个（死命令）

制作多个素材（图片 + meta + 导入）时，**必须严格串行**，禁止把多条提示词或生成请求一次打包提交。

并行/批量提交会严重损害提示词质量与输出效果。

## 强制流程

对每一个素材，独立走完整闭环，确认完成后再开始下一个：

1. 认领 **1** 个关键词，并用 **Gemini Interactions API**（`scripts/gemini_meta.py` / `npm run agent:meta`，模型默认 `gemini-3.6-flash` + `google_search`）生成元数据。可选替代：Together **Gemma 4**（`npm run agent:meta:gemma`，模型 `google/gemma-4-31B-it`，需 `TOGETHER_API_KEY`）
2. 使用返回的 `meta.imagePrompt` **单独**生成该素材图片（一次只调一次图像生成）。保存文件名必须与 `meta.imagePageTitle` 一致（去掉 `<>:"/\|?*` 等非法字符，保留 Title Case；不要用关键词当文件名）
3. 导入该素材（含尺寸变体等）：`node scripts/agent-import.mjs …`
4. 确认成功后，再开始下一条

用户从站点下载的文件名同样使用内容标题（`imagePageTitle` / `asset.title`），不要用带随机码的 slug。

N 个素材 = N 轮独立任务，不是一轮里做 N 个。

## 禁止

- 一次生成/提交多条 image prompt
- 并行调用多次图像生成
- 手写批量 meta 再批量出图（meta 由 Gemini 或 Together Gemma 4 逐条生成；出图与导入必须一条一条完成）
- 把「批量」理解成「同时做多个」

## 允许

- 用户明确要求「再生成 N 个」时：仍按上面流程 **串行做完 N 个**
- 仅查询关键词列表等只读准备步骤可先做，但 meta / 出图 / 导入仍一次一条
