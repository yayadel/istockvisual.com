---
description: 批量制作素材时必须一次只做一个，禁止并行/批量提交提示词
alwaysApply: true
---

# 批量内容制作：一次一个（死命令）

制作多个素材（图片 + meta + 导入）时，**必须严格串行**，禁止把多条提示词或生成请求一次打包提交。

并行/批量提交会严重损害提示词质量与输出效果。

## 强制流程

接到「做 N 个」时（N 为变量）：

1. **先锁定 N 个话题**（线上 D1）：`node scripts/agent-prepare.mjs --count=N`  
   同一批写入 `used=1` + 共用 `lockBatchId`，另开进程认领时看不到这批。本地默认已指向线上。
2. 对批次里每一个 pending 关键词，独立走完整闭环，确认完成后再开始下一个：
   - 用 Together **Gemma 4**（`npm run agent:meta:gemma`）生成元数据；仅当用户明确要求时才用 Gemini（`npm run agent:meta`）
   - 使用返回的 `meta.imagePrompt` **单独**生成该素材图片（一次只调一次图像生成）。保存文件名必须与 `meta.imagePageTitle` 一致（去掉 `<>:"/\|?*` 等非法字符，保留 Title Case；不要用关键词当文件名）
   - 导入该素材（含尺寸变体等）：`node scripts/agent-import.mjs …`
3. 中途放弃整批未完成话题：`node scripts/agent-release.mjs`（按 batchId 释放无内容锁）

用户从站点下载的文件名同样使用内容标题（`imagePageTitle` / `asset.title`），不要用带随机码的 slug。

N 个素材 = 1 次批量锁定 + N 轮串行制作，不是一轮里并行做 N 个。

## 禁止

- 一次生成/提交多条 image prompt
- 并行调用多次图像生成
- 手写批量 meta 再批量出图（meta 由 Gemini 或 Together Gemma 4 逐条生成；出图与导入必须一条一条完成）
- 把「批量」理解成「同时做多个」

## 允许

- 用户明确要求「再生成 N 个」时：先 `agent-prepare --count=N`，再串行做完 N 个
- 仅查询关键词列表等只读准备步骤可先做，但 meta / 出图 / 导入仍一次一条

Cloud Agent：导入打线上 Worker，密钥走 Cursor Secrets，详见仓库根目录 `AGENTS.md`。
